import FrontNavBar from '@/components/Navigation/Front';

const Header = () => {
    return (
        <div>
            <FrontNavBar />
        </div>
    );
};

Header.displayName = 'Front Header';
export default Header;
